Setup Instructions:

1) `$ mkvirtualenv -p /usr/bin/python3 beer`
2) created `requirements.txt` and ran `$ pip install requirements.txt`
3) created `Makefile` and pasted from `swapi` project
4) `git init` and created repo on GitHub to link two together
5) `$ django-admin startproject beer`
6) `$ python manage.py startapp beerapp`
7) `$ source ./private_export_keys.sh`
8) `$ make makerunserver`
9) runtime.txt to define python version
10) Procfile to tell how to run the app (for heroku)